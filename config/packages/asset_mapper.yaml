framework:
    asset_mapper:
        # The paths to make available to the asset mapper.
        paths:
            - assets/
    assets:
        packages:
            images:
                version: 'v1'
                base_path: '/assets/images'
