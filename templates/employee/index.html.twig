{% extends 'baseDash.html.twig' %}

{% block title %}Employé{% endblock %}


{% block body %}

    <div class="containerAdmin">
        <div class="row dash">
            {% include "_partials/_adminnav.html.twig" %}
            <main class="col-9 border bg-white p-2">
                <h1>Espace employé</h1>
                {% if username != 'Invité' %}
                    <p class="hello">Bonjour, {{ username }} !</p>
                {% else %}
                    <p class="hello">Bonjour, Invité !</p>
                {% endif %}
                <img class="mainDash" src={{ asset('images/soigneur2.jpg')}} alt="Bureau">
                {# <img src={{ asset('images/admin.jpg')}} alt="Bureau"> #}
            </main>
        </div>
    </div>


{% endblock %}