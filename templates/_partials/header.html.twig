<header>

    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <div class="navbar-brand">
                <img src={{ asset('/images/vecteurs/Logo.png')}} alt="Logo lion"></img>
                <div class="hheader">Zoo Arcadia</div>
            </div>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
                </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <a class="nav-link active" aria-current="page" href={{path ('app_home')}}><i class="fa-solid fa-house"></i></a>
                <a class="nav-link" href={{path('app_services')}}>Services</a>
                <a class="nav-link" href={{path('app_habitats')}}>Habitats</a>
                <a class="nav-link" href={{path('app_avis')}}>Avis</a>

                {% if app.user %}
                    <a class="nav-link" href={{path('app_logout')}}>Déconnexion</a>
                {% else %}
                    <a class="nav-link" href={{path('app_login')}}>Connexion</a>
                {% endif %}
            </div>
        </div>
    </nav>
    
</header>