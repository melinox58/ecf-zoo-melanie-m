{% extends 'baseDash.html.twig' %}

{% block title %}Ajouter un animal{% endblock %}


{% block body %}

    <div class="containerAdmin">
        <div class="row dash">
            {% include "_partials/_adminnav.html.twig" %}

            <main class="col-9 border bg-white p-2" id="anim">
                <h4>Ajouter un nouvel animal</h4>

                    {{ form_start(form) }}
                        {{ form_widget(form) }}
                    {{ form_end(form) }}

            </main>
        </div>
        <a class="btn btn-secondary mt-3 btnAnim" href="{{ path('app_admin_anim') }}">Retour à la liste</a>
    </div>
{% endblock %}
