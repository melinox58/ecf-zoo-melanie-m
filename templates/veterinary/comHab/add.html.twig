
{% extends 'base.html.twig' %}

{% block title %}Ajouter un commentaire habitat{% endblock %}

{% block body %}
<div class="containerAdmin">
    <div class="row">
        {% include "_partials/_adminnav.html.twig" %}

        <main class="col-9 border bg-white p-2">
    <h1>Ajouter un commentaire pour l'habitat</h1>

    {{ form_start(form) }}
        {{ form_row(form.comment) }}
        {{ form_row(form.submit) }}
    {{ form_end(form) }}

    </main>
    </div>
    <a href="{{ path('app_emp_anim') }}">Retour à la liste</a>
</div>
{% endblock %}
